apiVersion: v1
kind: Service
metadata:
  name: {{ include "24hr-story.fullname" . }}
  labels:
    {{- include "24hr-story.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "24hr-story.selectorLabels" . | nindent 4 }} 